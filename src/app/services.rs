//! macOS Services integration for MacHelix
//!
//! This module handles integration with macOS Services menu.

use anyhow::Result;

/// Register application services
pub fn register_services() -> Result<()> {
    // TODO: Register services with macOS
    Ok(())
}

/// Handle service request for new document
pub fn handle_new_document_service() -> Result<()> {
    // TODO: Handle new document service request
    Ok(())
}

/// Handle service request for opening selection
pub fn handle_open_selection_service() -> Result<()> {
    // TODO: Handle open selection service request
    Ok(())
}